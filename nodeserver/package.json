{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is a node server for a i-chat application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rohit",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.4",
    "socket.io": "^2.5.0"
  }
}
